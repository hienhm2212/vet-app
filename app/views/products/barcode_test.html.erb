<!DOCTYPE html>
<html>
<head>
  <title>Barcode Test - <%= @product.name %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      background: white;
    }
    .barcode-container {
      text-align: center;
      margin: 20px;
      padding: 20px;
      border: 1px solid #ccc;
    }
    .barcode-image {
      max-width: 100%;
      height: auto;
    }
    .info {
      margin-top: 10px;
      font-size: 12px;
      color: #666;
    }
  </style>
</head>
<body>
  <h2>Barcode Test for: <%= @product.name %></h2>
  
  <div class="barcode-container">
    <h3>Real Scannable Barcode</h3>
    <div class="barcode-image">
      <%= image_tag barcode_product_path(@product), alt: "Barcode: #{@product.barcode}" %>
    </div>
    <div class="info">
      <p><strong>Barcode Value:</strong> <%= @product.barcode %></p>
      <p><strong>Product:</strong> <%= @product.name %></p>
      <p><strong>Barcode Number:</strong> <%= @product.barcode %></p>
      <p><strong>Price:</strong> <%= format_vnd(@product.price) %></p>
    </div>
  </div>
  
  <p><a href="javascript:history.back()">← Back</a></p>
</body>
</html>
